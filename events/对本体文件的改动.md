GriffonContinental Events:
删掉：griffonian.6和griffonian.7

country_event = {
	id = griffonian.6
	immediate = {log = "[GetDateText]: [Root.GetName]: event griffonian.6"}
	title = griffonian.6.t
	desc = griffonian.6.d
	picture = GFX_event_griffon_mountain
	fire_only_once = yes
	trigger = {
		NOT = { tag = GRW }
		is_major = yes
		GRW = {
			exists = yes
			OR = {
				has_added_tension_amount > 20
				has_country_flag = { flag = prywhen_aggressiveness value > 5 }
			}
		}		
		389 = { is_controlled_by = GRW }
		#NOT = { has_country_flag = GRW_event_fired }
		NOT = { has_government = communism }
		is_griffon_continent_griffon_country = yes

	}
	mean_time_to_happen = { days = 1 }
	option = {
		name = griffonian.6.a
		create_wargoal = { target = GRW type = annex_everything }
		#set_country_flag = GRW_event_fired
		set_country_flag = wants_to_contain_GRW
		ai_chance = {
			factor = 70
			modifier = {
				factor = 0.5
				has_government = democratic
			}
			modifier = {
				factor = 0
				has_government = communism
			}
		}
	}
	option = {
		name = griffonian.6.b
		add_political_power = 100
		#set_country_flag = GRW_event_fired
		ai_chance = { factor = 30 }
	}
}
country_event = {
	id = griffonian.7
	immediate = {log = "[GetDateText]: [Root.GetName]: event griffonian.7"}
	title = griffonian.7.t
	desc = griffonian.7.d
	picture = GFX_event_griffon_mountain

	trigger = {
		NOT = { 
			tag = GRW 
			is_in_faction_with = GRW
			is_subject_of = GRW
		}
		GRW = { 
			exists = yes
			OR = {
				has_war_with = GRD
				has_war_with = JER
				has_war_with = WNG
			}
		}
		is_subject = no
		is_neighbor_of = GRW
		NOT = { has_war_with = GRW }
		NOT = { has_government = communism }
		389 = { is_controlled_by = GRW }
		NOT = { has_country_flag = GRW_event_fired2 }
		is_griffon_continent_griffon_country = yes
	}
	mean_time_to_happen = { days = 2 }
	option = {
		name = griffonian.7.a
		create_wargoal = { target = GRW type = annex_everything }
		set_country_flag = GRW_event_fired2
		set_country_flag = wants_to_contain_GRW
		ai_chance = {
			factor = 5
			modifier = {
				factor = 2
				has_government = fascism
			}
			modifier = {
				factor = 5
				is_major = yes
			}
		}
	}
	option = {
		name = griffonian.7.b
		add_political_power = 50
		set_country_flag = GRW_event_fired2
		ai_chance = {
			factor = 10
			modifier = {
				factor = 20
				has_government = communism
			}
			modifier = {
				factor = 5
				has_government = democratic
			}
			modifier = {
				factor = 3 
				is_major = no
			}
		}
	}
	option = {
		name = griffonian.7.c
		trigger = {
			has_government = communism
			is_major = no
			is_subject = no
		}
		GRW = { country_event = generic.2 }
		set_country_flag = GRW_event_fired2
		ai_chance = {
			factor = 200
			modifier = {
				factor = 0
				OR = {
					is_major = yes
					is_in_faction = yes
					owns_state = 382
					has_opinion = {
						target = GRW
						value < 50
					}
				}
			}
		}
	}
}

Gryphian_Host_events:
删除
country_event = {#Annex D01 into GRY if the neutrals rise up but lose, or if the fascists come to power but manage to lose their civil war
	id = gryphianhost.138
	immediate = {log = "[GetDateText]: [Root.GetName]: event gryphianhost.138"}
	title = gryphianhost.138.t
	desc = gryphianhost.138.d
	picture = GFX_event_griffons_machine_gun

	trigger = {
		original_tag = GRY
		NOT = { tag = GRY }
		OR = {
			has_government = communism
			has_government = democratic
		}
		GRY = {
			OR = {
				has_country_flag = GRY_neutral_rebellion
				has_completed_focus = GRY_prevent_ruina
			}
		}
		has_civil_war = no
	}
	fire_only_once = yes
	mean_time_to_happen = {
		days = 7
	}

	option = {#Revert to being GRY
		name = gryphianhost.138.a
		if = {
			limit = {
				has_government = democratic
			}
			GRY = {
				set_politics = { ruling_party = democratic elections_allowed = yes }
				promote_character = {
					character = GRY_borad_grifnitsky
					ideology = liberalism
				}
				complete_national_focus = GRY_volnoye
				annex_country = { target = ROOT }
			}
		}
		if = {
			limit = {
				has_government = communism
			}
			GRY = {
				set_politics = { ruling_party = communism elections_allowed = no }
				clr_country_flag = GRY_neutral_rebellion
				promote_character = GRY_cornilo_khurshevich
				load_focus_tree = { tree = GRY_focus keep_completed = yes }
				complete_national_focus = GRY_first_universale
				annex_country = { target = ROOT }
			}
		}
	}
}